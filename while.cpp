//usada pra quando não sabe quantas vezes ao certo repetir

#include <stdio.h>
#include <conio.h>
#include <math.h>
#include <locale.h>

main ()
 { setlocale(LC_ALL, "Portuguese");
  int contador = 1;
   
    while (contador <= 11)
     {
     	printf("\nBoa noite");
     	contador++;
	 }
}
